<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="description" content="Welcome to my website!">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <meta property="og:title" content="{{ block('title') }}">
    <meta property="og:image" content="{{ asset('build/images/billboard/code.png') }}">
    <link rel="stylesheet" href="">
    <link rel="icon" type="image/svg+xml" href="{{ asset('build/images/icons/favicon.svg') }}">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>

{% include 'components/_header.html.twig' %}

<main>

    <article{% if css is defined %} class="{{ css }}"{% endif %}{% if controller is defined %} data-controller="{{ controller }}"{% endif %}>
        {% block maincontent %}
        {% endblock %}
    </article>

</main>

{% include 'components/_footer.html.twig' %}

</body>
</html>
